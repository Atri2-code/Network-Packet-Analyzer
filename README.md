# Network-Packet-Analyzer
Python tool capturing and classifying live TCP/UDP/ICMP/DNS network traffic by protocol, port and service — with enterprise port mapping, TCP flag parsing, and traffic summary statistics.
